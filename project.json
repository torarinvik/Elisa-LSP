{
  "version": "0.1.0",
  "include-dirs": ["src"],
  "toolchain": {
    "compiler": "stage0 elisacore ($HOME/.elisac/elisac-stage0)",
    "compiler_note": "stage1 self-hosted backend declines LSP constructs; do not use for this target (see docs/development.md)",
    "frontend_include": "../../Elisa-compiler/src/semantic/semantic.elisa (override via ELISA_COMPILER_ROOT)",
    "link": "host clang + weak profiling fallback (generated by build.sh)"
  },
  "targets": {
    "elisa-lsp": {
      "entry": "src/main.elisa",
      "emit": "obj",
      "output": "build/elisa-lsp.o",
      "opt": "O2",
      "profiles": {
        "release": "O2",
        "debug": "O0"
      }
    }
  }
}
